{
  "name": "blog-app",
  "version": "1.0.0",
  "description": "blog-app application showcasing graphql with apollo server with integration with postgreSql, prisma and using typescript",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "nodemon --watch './**/*.ts' --exec 'ts-node' src/index.ts"
  },
  "keywords": [
    "graphql",
    "apollo",
    "server",
    "typescript",
    "postgresql",
    "prisma"
  ],
  "author": "sritaj kumar patel",
  "license": "ISC",
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "prisma": "^4.0.0"
  }
}
